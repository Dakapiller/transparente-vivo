# Transparente Vivo Website

Pixel-focused Vite, React, and TypeScript implementation of the Transparente Vivo Figma website.

## Scripts

- `npm run dev` starts the local development server.
- `npm test` runs content and scroll-lock contract tests.
- `npm run build` type-checks and builds the production bundle.
- `npm run lint` runs ESLint.
